import 'dart:ui';

abstract class BaseColors {
  Color get primary;
  Color get secondary;
  Color get whiteColor;
  Color get greyLight;
  Color get greyMiddle;
  Color get primaryText;
  Color get primaryLink;
  Color get cursorColor;
  Color get normalOutlineInput;
  Color get primaryBackground;
  Color get primaryBackgroundInput;
}
